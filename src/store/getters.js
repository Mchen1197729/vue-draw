export default {
  // 形状的下标
  shapeIndex: state => state.shape.shapeIndex,
  // 形状的颜色
  shapeColor: state => state.header.shapeColor,
  // 形状的虚线/实线
  shapeLineDash: state => state.header.shapeLineDash
}
